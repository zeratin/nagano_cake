<%= render 'layouts/header' %>
<div class="container">
  <div class="row">

    <div class="col-xs-3">
        <%= render 'items/search' %>
    </div>

    <div class="col-xs-9">

     <h1>カート商品</h1>

     <% @cart_items.each do |cart_item| %>
     <%= cart_item.name %>
     <%= cart_item.amount %>
     <% end %>

    </div>
  </div>
</div>