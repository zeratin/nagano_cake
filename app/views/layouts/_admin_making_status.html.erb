<%= form_with model: @order_detail, url: admins_order_path(order), method: :patch, local:true do |f| %>
  <%= f.select :making_status, [["製作不可", "製作不可"], ["製作待ち", "製作待ち"], ["製作中", "製作中"], ["製作完了", "製作完了"]] %>

  <%= f.submit "更新", class: 'btn btn-primary' %>
<% end %>