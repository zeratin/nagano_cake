<%= render 'layouts/header' %>
<div class="container">
  <div class="row">

    <div class="col-xs-3">
        <%= render 'items/search',  genres: @genres %>
    </div>

    <div class="col-xs-9">

        <% @items.each do |item| %>
        <tr>
          <td>
            <%= link_to item_path(item) do %>
              <%= attachment_image_tag item, :image_id, :fill, 180, 120, format: 'jpeg' %><br>
              <%= item.name %><br>
            <% end %>
          </td>
          <td> ¥<%= item.price %><br></td>
        <% end %>

                </div>
            </tbody>
           </table>
    </div>

  </div>
  </div>

  <!--見本-->

