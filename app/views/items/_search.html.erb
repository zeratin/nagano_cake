<h4 class="text-center">ジャンル検索</h4>
<table class="table table-hover table-inverse">
  <% @genres.each do |genre| %>
    <tr>
      <td class="text-center">
        <%= link_to "#{genre.name}", items_path(search: genre.id) %>
      </td>
    </tr>
  <% end %>
</table>